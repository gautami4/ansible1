- name: ngnix server
  hosts: web
  become: yes # root user access
  tasks: 
  - name: install server
    ansible.builtin.package:
      name: ngnix
      state: present
  - name: start service
    ansible.builtin.service:
      name: ngnix
      state: started
      
