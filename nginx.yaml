- name: nginx server
  hosts: web
  become: yes # root user access
  tasks: 
  - name: install server
    ansible.builtin.package:
      name: nginx
      state: present
  - name: start service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes

